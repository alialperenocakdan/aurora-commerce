databaseChangeLog:
  - changeSet:
      id: 000-create-schema
      author: aurora
      changes:
        # Liquibase'in kendi izleme tablosu 'public' şemasında tutulur (application.yml'de
        # liquibase-schema: public), bu yüzden bu adım her zaman çalışabilir — 'auth' şeması
        # henüz yokken bile. Sonraki tüm changeset'ler bu şemanın var olduğunu varsayabilir.
        - sql:
            sql: "CREATE SCHEMA IF NOT EXISTS auth;"
      rollback:
        - sql:
            sql: "DROP SCHEMA IF EXISTS auth CASCADE;"
